# ooh-recommend — 프로젝트 지침 + Wiki 스키마

OOH(옥외광고) 매체 추천 챗봇. 상세 개요는 [CONTEXT.md](CONTEXT.md) 참조 (도메인/아키텍처/DB/배포).

---

# 📚 Knowledge Wiki 운영 규칙

이 레포의 `docs/`는 **LLM Wiki**다 — 매번 원본을 다시 뒤지지 않도록, 정리된 지식을 **누적**하는 마크다운 위키. 정보를 새로 알게 되거나 결정하면 위키에 반영해 시간이 지날수록 쌓이게 한다.

## 3계층 구조

1. **Raw sources** — 원본. 노트북(`todolist` 레포), 데이터(HOO 922건 xlsx, 서울 상권), 외부 자료. **수정하지 않음.**
2. **Wiki** — `docs/` 안의 마크다운. `plans/`(계획·PRD), `policies/`(화면 정책), `api/`, 프런트 문서 등.
3. **Schema** — 이 파일(`CLAUDE.md`) + 개요 [CONTEXT.md](CONTEXT.md).

## 특수 파일 (항상 최신 유지)

- **[docs/index.md](docs/index.md)** — 전체 문서 카탈로그. 문서를 추가/이동/삭제하면 **여기부터** 갱신.
- **[docs/log.md](docs/log.md)** — 시간순 로그(최신이 위). 자료 ingest·주요 결정·작업 완료를 한두 줄로 append.

## 3가지 작업

### Ingest — "이 자료 정리해서 위키에 넣어줘"
1. 원본을 읽고 요약한다.
2. 알맞은 위치에 마크다운 페이지를 만들거나 기존 페이지를 갱신한다 (계획/PRD → `docs/plans/YYYY-MM-DD-제목.md`, 화면 정책 → `docs/policies/`, API → `docs/api/`).
3. 관련 기존 페이지에 상대 링크를 건다 (교차 참조).
4. **[index.md](docs/index.md) 항목 추가 + [log.md](docs/log.md)에 한 줄 기록.**

### Query — "X에 대해 지금까지 정리된 거 알려줘"
1. 먼저 [index.md](docs/index.md)로 관련 페이지를 찾고, 그 페이지들을 근거로 답한다 (원본 재탐색은 위키에 없을 때만).
2. 답이 재사용 가치가 있으면 새 페이지로 정리하거나 기존 페이지를 보강하고 index/log 갱신.

### Lint — "위키 점검해줘"
- 모순되는 서술, 고아 페이지(index에 없는 파일 / 아무도 링크 안 하는 페이지), 빠진 교차링크, 오래된 내용(코드와 어긋난 서술)을 찾아 보고. 발견 시 수정 제안.

## 컨벤션

- 링크는 **상대경로 마크다운 링크** (`[제목](docs/plans/....md)`). GitHub에서 그대로 렌더된다.
- 날짜는 실제 현재 날짜 확인 후 `YYYY-MM-DD` (지식 컷오프로 가정 금지).
- 화면 단위 정책서는 `docs/skills/prd-policy-writer` 스킬 컨벤션을 따른다.
- 위키 문서는 요청 시에만 커밋 (아래 작업 규칙 준수).

---

# 작업 규칙 (요약)

- 코드 수정 전 해당 파일 Read 먼저. 요청한 것만 구현(MVP), 기존 스타일 유지.
- **커밋/푸시는 사용자가 명시적으로 지시할 때만.**
- 포트: postgres **5433**, backend **8001**, front dev 3000 (todolist 5432/8000과 분리).
- 상세 아키텍처/그래프 흐름/DB/사용자 결정 메모는 [CONTEXT.md](CONTEXT.md)에 있으니 중복 서술하지 말고 참조·갱신.
