## 기획내용 추가 
frontend/app/(main)/ad-recommend-v2/_components/AdRecommendV2Panel.tsx MergedSlotsView 를 채팅 가장 상위에서 계속보여주고,                       
  슬롯 뱃지 각각에 x버튼을 달아서, 클릭시 제거하고 남은 조건으로 검색리스트를 다시 뽑아서 제공하기 


금액 슬롯 추가. 이하값 필터링 


  ## 클로드 계획 

  1. 백엔드 — 새 엔드포인트 POST /recommend/v2/slot/remove 추가
    - 세션 컨텍스트에서 특정 (category, code) 제거
    - 남은 슬롯으로 동일한 파이프라인 재실행 (0개→chat, 1개→need_more, 2개↑→list)
    - 기존 stream과 동일한 SSE 포맷 → 프론트 핸들러 재사용
  2. 프론트엔드 — AdRecommendV2Panel.tsx
    - 마지막 어시스턴트 메시지에서 currentSlots 도출 (memo)
    - 헤더 바로 아래에 항상 보이는 상단 슬롯 바 추가
    - 슬롯을 카테고리×코드 단위로 분해해 뱃지 + X 버튼 렌더
    - X 클릭 → 새 엔드포인트 호출 → 사용자 안내 버블 + 어시스턴트 응답 추가
    - 어시스턴트 버블 안의 중복 MergedSlotsView 제거 (상단으로 이전)
    - 스트림 처리 로직(두 곳에서 동일하게 사용)을 작은 헬퍼로 추출