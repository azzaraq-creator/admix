# AI 매체추천 탭 정책서

> **이 문서는 "이 화면이 어떤 규칙으로 동작하는가"를 정의합니다.**
> 디자인(여백·색·컴포넌트 모양)은 Figma에 두고, 여기서는 기능·정책·흐름만 다룹니다.

| 항목 | 내용 |
|---|---|
| **화면명** | AI 매체추천 탭 |
| **경로 / 라우트** | /ad-recommend |
| **Figma (참조용)** | [옥외광고플랫폼 — node 759-99870](https://www.figma.com/design/z4QnuGbOaDJzqd5kyx5Udv/%EC%98%A5%EC%99%B8%EA%B4%91%EA%B3%A0%ED%94%8C%EB%9E%AB%ED%8F%BC?node-id=759-99870&m=dev) |
| **작성자** | (기획자 이름) |
| **상태** | `draft` |
| **최종 수정** | 2026-06-05 |
| **관련 화면** | 로그인/회원가입, 사업자 등록, 제안서 생성, 매체 상세 |

---

## 1. 화면 목적 (Why)

광고주가 자연어로 광고 조건을 입력하면, 챗봇이 대화를 통해 조건을 모아 최적의 OOH(옥외광고) 매체를 추천해 주는 화면이다. 검색 폼을 채우는 대신 대화로 매체를 찾고, 추천 결과를 제안서로 이어가게 한다.

- **핵심 가치**: 광고 지식이 없어도 대화만으로 적합한 옥외광고 매체를 찾는다.
- **주 사용자**: 광고를 집행하려는 광고주(비회원 ~ 기업회원).
- **성공 기준**: 추천 리스트 도달률, 추천 → 제안서 생성 전환율.

---

## 2. 사용자 & 권한 정책

채팅 사용량은 사용자 등급에 따라 다르며, **채팅 토큰(발화 횟수)** 으로 제한한다.

| 사용자 구분 | 접근 | 제한 / 한도 | 한도 도달 시 동작 |
|---|---|---|---|
| 비회원 | 채팅 사용 가능 | **채팅 토큰 5회** | 5회 소진 시 추가 발화 차단 → **로그인 유도** |
| 개인회원 | 채팅 사용 가능 | **30회** | 소진 시 추가 발화 차단 → 안내(상위 등급/기업 전환 등) |
| 기업회원 (사업자 미등록) | 채팅 사용 가능 | **100회** | 소진 시 추가 발화 차단 → **사업자 등록 유도** |
| 기업회원 (사업자 등록 완료) | 채팅 사용 가능 | **무제한** | 해당 없음 |

> **카운팅 규칙 (논의 필요 — 9번 참조)**: 무엇을 1회로 셀지(사용자 발화 1건 = 1회 가정), 리셋 주기(누적/월/일), 비회원→회원 전환 시 잔여 횟수 처리 방식은 아직 미확정.

---

## 3. 핵심 기능 (What)

### 3.1 매체 추천 챗봇

- **무엇을**: 사용자의 자연어 발화에서 광고 조건(슬롯)을 추출하고, 조건이 충분해지면 매체를 추천한다.
- **동작 규칙**: 발화 → 슬롯 추출 → 슬롯 개수 판단 → 부족하면 추가 질문, 충분하면(2개 이상) 추천 리스트 생성.
- **제약**: **슬롯이 2개 이상** 모여야 추천 리스트를 만든다. 그 전까지는 조건을 더 요청한다.

### 3.2 추천 리스트

- **무엇을**: 조건에 맞는 매체 목록을 카드 형태로 제시한다.
- **동작 규칙**: 슬롯 2개 이상 충족 시 생성. 결과가 있으면 리스트 + 지도를 함께 노출하고, 이어서 **제안서 생성을 권유**한다.
- **제약**: 결과 0건이면 리스트 대신 "결과 없음" 안내.

### 3.3 지도 표시

- **무엇을**: 추천된 매체의 위치를 지도에 표시한다.
- **동작 규칙**: **추천 리스트가 발생할 때** 지도 표시가 함께 트리거된다.
- **제약**: 추천 결과가 없으면 지도는 표시되지 않거나 기본 상태.

### 3.4 제안서 생성 권유

- **무엇을**: 추천 리스트 이후 제안서 생성으로 넘어가도록 유도한다.
- **동작 규칙**: 추천 리스트 노출 직후 권유 메시지/액션 노출.
- **제약**: (제안서 생성 가능 조건/권한은 제안서 화면 정책서에서 정의 — 논의 필요)

---

## 4. 사용자 플로우 (How)

```
탭 진입
 └─ 채팅 입력
     ├─ 토큰 잔여 없음              → 발화 차단 + 등급별 유도(로그인/사업자등록)
     └─ 토큰 잔여 있음 → 슬롯 추출
          ├─ 슬롯 0~1개            → 조건 더 요청 (추가 질문)
          └─ 슬롯 2개 이상
               ├─ 결과 0건         → "결과 없음" 안내
               └─ 결과 있음        → 추천 리스트 + 지도 표시 → 제안서 생성 권유
```

핵심 단계별 규칙:

1. **진입** — 채팅 인터페이스 노출. 등급에 따른 잔여 토큰 상태 반영.
2. **발화 입력** — 토큰 잔여 확인이 먼저. 없으면 등급별 유도 동작으로 분기.
3. **슬롯 추출** — 발화에서 광고 조건을 추출해 누적.
4. **추천 판단** — 슬롯 2개 이상이면 추천, 아니면 조건을 더 요청.
5. **결과 노출** — 결과가 있으면 리스트 + 지도 동시 표시.
6. **다음 행동 권유** — 제안서 생성으로 유도.

---

## 5. 화면 상태 정의

| 상태 | 발생 조건 | 노출 / 동작 |
|---|---|---|
| 빈 상태(empty) | 첫 진입, 대화 없음 | 입력 가이드/예시 발화 노출 |
| 로딩(loading) | 추천 검색 중 | 검색 중 표시(스피너 등) |
| 조건 부족 | 슬롯 0~1개 | 조건을 더 요청하는 메시지 |
| 성공(result) | 슬롯 2개+ & 결과 있음 | 추천 리스트 + 지도 + 제안서 권유 |
| 결과 없음 | 슬롯 2개+ & 결과 0건 | "조건에 맞는 매체가 없습니다" 안내 |
| 제한 도달(limit) | 잔여 토큰 0 | 발화 차단 + 등급별 유도(로그인/사업자등록) |
| 에러(error) | 통신/서버 오류 | 오류 안내 + 입력 보존 (논의 필요) |

---

## 6. 비즈니스 규칙 / 정책

- 채팅 토큰 한도 — 비회원 **5회**, 개인회원 **30회**, 기업회원(사업자 미등록) **100회**, 기업회원(사업자 등록) **무제한**.
- 토큰 소진 시 추가 발화를 **차단**하고 등급별 유도 동작 실행: 비회원 → 로그인, 기업 미등록 → 사업자 등록.
- 추천 리스트는 **슬롯 2개 이상** 충족 시에만 생성한다.
- 추천 리스트 발생 시 **지도 표시를 함께 트리거**한다.
- 추천 리스트 노출 후 **제안서 생성을 권유**한다.

---

## 7. 예외 & 엣지 케이스

- 통신/서버 오류 시 → 오류 안내 + 사용자 입력 보존(재입력 부담 최소화). (구체 문구·재시도 방식 논의 필요)
- 세션 도중 로그인/등급 변경 → 잔여 토큰·한도 즉시 재계산.
- 비회원이 5회 소진 직전/직후 로그인 → 잔여 횟수 이관 규칙 (논의 필요).
- 동일 조건 반복 발화 / 새로고침 → 진행 중 대화·슬롯 보존 여부 (논의 필요).

---

## 8. 논의 필요 (Open Questions / 결정 로그)

> 기획·디자인·개발이 **아직 합의하지 못한 것**. 정해지면 아래 "결정됨"으로 옮깁니다.

**미결정**

- [ ] 토큰 1회의 정의 — 사용자 발화 1건 기준인가? 추천 결과 1건 기준인가?
- [ ] 토큰 한도 리셋 주기 — 누적 영구인가, 월/일 단위 리셋인가?
- [ ] 비회원 → 회원 전환 시 잔여 토큰(또는 사용 이력) 이관 여부.
- [ ] 개인회원 30회 소진 후 유도 동작 — 기업 전환? 추가 결제? 단순 안내?
- [ ] "슬롯"의 정확한 정의와 종류(지역/예산/매체/제품/타깃/목적 등) — 추천 모듈 정책서와 정합 확인 필요.
- [ ] 지도 핀 클릭 시 동작(매체 상세 이동 등).
- [ ] 제안서 생성 권한·조건 — 등급별로 다른가?
- [ ] 에러 시 재시도 UX 및 입력 보존 범위.

**결정됨**

- _(아직 없음)_
